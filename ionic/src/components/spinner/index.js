import { IonSpinner } from '@ionic/react';
import React from 'react';
import './Spinner.css';

const Spinner = () => (

  <IonSpinner name="dots" className="spinnerOptions" />

);

export default Spinner;
